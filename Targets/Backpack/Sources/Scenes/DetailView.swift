import SwiftUI

struct DetailView: View {
    var body: some View {
        VStack {
            Text("Show the details of the Pokemon")
        }
        .padding()
    }
}

struct DetailView_Previews: PreviewProvider {
    static var previews: some View {
        DetailView()
    }
}
